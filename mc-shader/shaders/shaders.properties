# Minimal properties to hint passes and keep compatibility

# Enable composite pass
composite.enabled=true

# Water pass uses gbuffers_water
gbuffers_water.enabled=true

# Keep default buffer formats; aim for wide compatibility

